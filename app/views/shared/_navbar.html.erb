<nav id="nav_bar" class="navbar navbar-default" role="navigation">
      <div class="container-fluid">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <%= link_to "Frita", root_path, class: "navbar-brand", id: "site_title" %>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
       <!--     <li><a href="#">Link</a></li> -->
       <% if session[:user_id].nil? %>
            <li><a href="#" class="btn" data-toggle="modal" data-target="#basicModal">New Account</a></li>

            <%= render partial: 'shared/create_user_form' %>

    <% end %>


      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user && !admin? %>
          
          <li><%= link_to "Welcome, #{current_user.name.capitalize}", user_orders_path(current_user), class: "welcome-link"%></li>
          <li><%= link_to 'Settings', user_path(current_user) %></li>
          <li><%= link_to 'Logout', logout_path %></li>
          <li>
            <button type="button" id="cart-button" class="btn btn-default btn-md">
            <span class="glyphicon glyphicon-shopping-cart"></span><%= @cart.line_items.count %>
          </button>
        </li>
        <% elsif admin? && current_user %>
        <li><p id="user_welcome">Welcome, <span class="user-name"><%= link_to current_user.name.capitalize, user_orders_path(current_user) %></span></p></li>
          <li><%= link_to 'Administrator Menu', admin_path %></li>
          <li><%= link_to 'Logout', logout_path %></li>
        <% else %>
          <%= form_tag(login_path, { style: "padding-top: 10px;" }) do %>
          <%= text_field_tag :name, "", placeholder: 'Name' %>
          <%= password_field_tag :password, "", placeholder: 'Password', class: 'focus' %>
          <%= submit_tag 'Login', class: "btn btn-sm" %>
          <% end %>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->

</nav>
